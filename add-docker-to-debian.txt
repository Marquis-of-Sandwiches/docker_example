Everything from here https://docs.docker.com/engine/install/debian/
I su to root.
apt  install ca-certificates curl gnupg.. Already installed for me
install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/debian/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
chmod a+r /etc/apt/keyrings/docker.gpg
echo \
  "deb [arch="$(dpkg --print-architecture)" signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
  "$(. /etc/os-release && echo "$VERSION_CODENAME")" stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
apt update
apt install docker-ce
#minikube uses kvm/qemu
systemctl disable docker
create directory to do image build
mkdir ~/docker-build
cd ~/docker-build
#Dockerfile is in docker-build
docker build -t mp-prometheus:1.0 .
docker save -o mp-prometheus.tar mp-prometheus:1.0
